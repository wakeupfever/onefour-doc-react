import styled from 'styled-components'

export const SingerDivAlias = styled.div`
  height: calc(100vh - 44px - 44px);
  background-color: red;
  overflow: auto;
  .scroll {
    height: 1000px;
  }
`